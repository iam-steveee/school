document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const id=a.getAttribute('href');if(id.length>1){const el=document.querySelector(id);if(el){e.preventDefault();el.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'start'})}}}));document.querySelectorAll('.section,.hero,.intro').forEach((el,i)=>{el.style.opacity='0';el.style.transform='translateY(18px)';el.style.transition='opacity .65s ease,transform .65s ease';el.style.transitionDelay=Math.min(i*20,120)+'ms'});const show=el=>{el.style.opacity='1';el.style.transform='translateY(0)'};if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){show(e.target);io.unobserve(e.target)}}),{threshold:.06});document.querySelectorAll('.section,.hero,.intro').forEach(el=>io.observe(el))}else document.querySelectorAll('.section,.hero,.intro').forEach(show);document.querySelectorAll('img').forEach(img=>img.addEventListener('error',()=>img.closest('figure')?.classList.add('broken')));});